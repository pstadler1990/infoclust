package vectorize

type Bow struct {
	word  string
	count int
}

func CreateVector(fPath string) {
	/* Reads in two bag of words of type Bow,
	   and creates two vectors of these files.
	   Missing words are added to the vector with count 0 */

}
